# Single source of truth for both Colab (!pip install) and local venv.
# See README §9 for setup instructions.
#
# NOTE: PyTorch and torchvision are intentionally NOT pinned here.
#   - Colab : pre-installed; do not reinstall (CUDA stack mismatch risk).
#   - Local : install the wheel matching your CUDA from
#             https://pytorch.org/get-started/locally
#   Code in this repo is verified against torch >= 2.3.

numpy>=1.26,<3.0
pandas>=2.0
scikit-learn>=1.3
matplotlib>=3.7
seaborn>=0.13
tqdm>=4.66
pyyaml>=6.0
pillow>=10.0
albumentations>=1.3,<2.0
captum>=0.7
kagglehub>=0.2
wandb>=0.27.2
